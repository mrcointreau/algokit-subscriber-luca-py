name: Build, Test and Publish Python

on: [workflow_call]

jobs:
  build-python:
    strategy:
      matrix:
        os: ["ubuntu-latest"]
        python: ["3.12", "3.13", "3.14"]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout source code
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - name: Setup Python environment
        uses: algorandfoundation/algokit-shared-config/.github/actions/setup-algokit-python@main
        with:
          python-version: ${{ matrix.python }}
          install-algokit: "false"

      - name: pytest + coverage
        shell: bash
        run: |
          set -o pipefail
          uvx algokit localnet start
          # NOTE: tests run serially (no pytest-xdist -n auto); they are live-localnet
          # integration tests with round-based assertions and are not parallel-safe.
          uv run pytest --junitxml=pytest-junit.xml --cov-report=term-missing:skip-covered --cov=src | tee pytest-coverage.txt
          uvx algokit localnet stop

      - name: pytest coverage comment - using Python 3.12 on ubuntu-latest
        if: matrix.python == '3.12' && matrix.os == 'ubuntu-latest'
        uses: MishaKav/pytest-coverage-comment@dd5b80bde6d16941f336518e92929e89069d8451 # v1.7.2
        continue-on-error: true # forks fail to add a comment, so continue any way
        with:
          pytest-coverage-path: ./pytest-coverage.txt
          junitxml-path: ./pytest-junit.xml

      - name: Upload test results and coverage artifacts
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: test-results-python-${{ matrix.python }}
          path: pytest-junit.xml
          retention-days: 30

      - name: Build package distribution
        run: uv build
